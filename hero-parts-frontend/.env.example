# Copy to .env and fill in values
VITE_API_BASE_URL=http://localhost:8080
# Claude API key — keep this server-side in production.
# Only use here for local development. Add a /api/v1/ai/* proxy in the Spring Boot
# backend before deploying so this key is never exposed to browsers.
VITE_CLAUDE_API_KEY=sk-ant-...
VITE_CLAUDE_MODEL=claude-sonnet-4-20250514
